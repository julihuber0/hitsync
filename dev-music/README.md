# dev-music

Drop a handful of MP3 files here (any you have the rights to use locally) to give the throwaway
dev Navidrome something to index — see `docs/local-development.md`. This directory's contents are
git-ignored; only this README is tracked.
